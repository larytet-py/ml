name: Option Signal Notifier

on:
  schedule:
    # GitHub cron is UTC; these two entries cover DST/standard-time.
    - cron: '1 20 * * 1-5' # 16:01 America/New_York when EDT
    - cron: '1 21 * * 1-5' # 16:01 America/New_York when EST
  workflow_dispatch:

jobs:
  run-notifier:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Gate on America/New_York 16:01
        id: ny_gate
        run: |
          set -euo pipefail
          ny_time="$(TZ=America/New_York date +%H:%M)"
          if [ "$ny_time" = "16:01" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.ny_gate.outputs.should_run == 'true'
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        if: steps.ny_gate.outputs.should_run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        if: steps.ny_gate.outputs.should_run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run notifier
        id: notifier
        if: steps.ny_gate.outputs.should_run == 'true'
        env:
          MARKET_STACK_API_KEY: ${{ secrets.MARKET_STACK_API_KEY }}
        run: |
          set -euo pipefail
          mkdir -p data
          python option_signal_notifier.py --config option_signal_notifier.config --summary-json notifier_summary.json | tee notifier_output.txt
          python .github/scripts/build_notifier_excerpt.py --summary-json notifier_summary.json

      - name: Commit updated ETF cache
        id: commit_cache
        if: steps.ny_gate.outputs.should_run == 'true'
        run: |
          set -euo pipefail
          git lfs install

          if [ ! -f data/etfs.csv ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if git diff --quiet -- data/etfs.csv; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f data/etfs.csv
          git commit -m "chore: update data/etfs.csv [skip ci]"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Send signal email (Resend API)
        if: steps.ny_gate.outputs.should_run == 'true' && steps.notifier.outputs.has_signal == 'true'
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          REPOSITORY: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          EXCERPT: ${{ steps.notifier.outputs.excerpt }}
        run: |
          set -euo pipefail
          python .github/scripts/send_signal_email.py
